extends layout

block content

  h1 #{title}: #{bookInstance.book.name}
  p Do you really want to delete this BookInstance?

  form(method='POST')
    div.form-group
      input#instanceid.form-control(type='hidden', name='instanceid', value=bookInstance._id )

      button.btn.btn-primary(type='submit') Delete